server:
  port: 8080
  servlet:
    context-path: /ebook

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/ebook"
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
